rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permite acesso apenas para usuários logados (igual ao seu banco de dados)
      allow read, write: if request.auth != null;
    }
  }
}